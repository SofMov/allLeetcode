struct Node{
    struct Node** further_nodes; //array of Node pointers
    int nodeNum; 
};

void setup(int* arr, int arrSize);

int minJumps(int* arr, int arrSize) {

}

void setup(int* arr, int arrSize){
    for(int i = 0; i < arrSize; i++){
        int next = nextNum(arr, arrSize, )
    }
}